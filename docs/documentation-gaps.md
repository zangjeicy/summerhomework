# 文档缺口清单

盘点基线：`dev@605fd015`，日期 2026-07-12。本清单区分“已存在但需核对”和“尚缺入口”，不把未核验推断写成技术事实。

## 当前覆盖情况

| 领域 | 已有入口 | 主要缺口或冲突 | 后续动作 |
| --- | --- | --- | --- |
| 项目概览 | `README.md`、`AGENTS.md` | README 面向用户，AGENTS 只有速览；缺少正式架构总览和端到端数据流 | 真实性核验后补架构入口 |
| 模块职责 | `AGENTS.md` 仓库速览 | 只有目录级职责，服务/仓储/API/Web 之间缺少稳定映射 | 007~010 报告先给源码锚点 |
| API | `docs/architecture/api_spec.json` | 机器规格存在，但课程所需的认证、管理员端点和典型响应矩阵不足 | CONFIG-AUTH-010 |
| 配置与部署 | `full-guide.md`、`DEPLOY.md`、LLM/设置文档、`.env.example` | 尚未明确哪些配置和入口属于 `v0.1.0` 教学 ZIP | `v0.1.0` manifest 与 010 |
| 测试与验收 | `tests/`、Web tests、CI gate | 缺少面向课程的功能真实性验收总表和固定演示路径 | 007~010 |
| 问题记录 | GitHub issue/PR 机制、`CHANGELOG.md` | 此前无课程问题编号与复验闭环 | 本任务已建立 `course-issues.md` |
| 版本与变更 | Git tag、`CHANGELOG.md`、Desktop package version | 课程版本、教学 ZIP 和产品/开发版本的映射尚不完整 | 为既有课程版本补 manifest，不改变 `v0.1.0` |
| 课程映射 | `v0.1.0` ZIP、PPT、RuyiWriter 控制状态 | 缺少 ZIP 代码路径 ↔ docs ↔ 测试 ↔ 截图 ↔ PPT 页面映射；旧控制状态仍为 15 页而正式 PPTX 为 32 页 | 007～010 + PPT 01 制作资料修订 |
| 演示数据 | 现有 reports/assets，以及工作区未跟踪 `examples/` 候选资产 | 是否完整、脱敏、许可明确、可重建尚未验收；未跟踪资产不纳入本任务 | PORTFOLIO-007 等专项核验 |
| AI 协作 | `AGENTS.md`、CodeGraph、`.claude/skills` | `.codex/skills/course-*` 指向错误项目；未跟踪 `.agents/skills` 形成潜在多真源 | 独立 AI 资产治理任务 |

## Codex 难以直接找到的信息

- 一个页面从前端组件到 API、Service、Repository、数据库表和测试的完整链路。
- 账户成本、收益、Token 统计等业务口径的权威定义与边界案例。
- Cookie、Bearer、首次设置密码、普通登录、管理员 API 的状态/权限矩阵。
- 课程截图中的数据是 Mock、空状态、演示数据还是真实运行结果。
- 某一 PPT 和某一源码/依赖/配置/数据库快照之间的一对一关系。

## 当前无法保证复现的流程

- 从 `v0.1.0` ZIP 复现 PPT 01/02/03 每个页面和业务口径的完整映射。
- 从 PPT 01 的生成源、模板和素材完整重建实际页集。
- 用固定演示数据稳定复现 Portfolio、Token、选股、回测、告警、通知和管理员页面全部状态。
- 在没有课程版本 manifest 的情况下判断一次 `dev` 变化影响 `v0.1.0` 还是后续增量课程。

## 适合写入 AGENTS.md 的长期规则候选

以下内容先作为候选，待后续实践稳定后再改根规则：

- 课程问题必须使用问题编号，并在 `dev` 修复后依次完成自动测试、页面和课程复验。
- 每个既有课程版本应记录教学 ZIP、已知 Git 映射、依赖、配置模板、演示数据、测试、截图、docs 与 PPT manifest；缺失映射写明“暂未确认”，不否定课程版本。
- 用户可见页面/API/配置/业务口径变化必须执行课程影响评估。

本轮不修改 `AGENTS.md`，避免把尚未经过专项核验的流程提前固化为硬规则。
